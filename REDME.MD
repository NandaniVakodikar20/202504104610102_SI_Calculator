# Simple Interest Calculator

A user-friendly web application to calculate Simple Interest based on Principal amount, Interest Rate, and Time (in years).


## Setup Instructions
1. Clone this repository to  local system.
2. Open the `index.html` file in any web browser to run the application.

---

## Jenkins Installation Guide using Docker

### 1. Run Jenkins Container
Execute the following command in your terminal to start a Jenkins LTS container:
```bash
docker run -d -p 8080:8080 -p 50000:50000 --name jenkins-server jenkins/jenkins:lts
